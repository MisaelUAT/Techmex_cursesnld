import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ServiceStyles {
  
  constructor(){}
  
  // WidthManager(element:HTMLElement,num_med:number,med:string){
  //   let container_style = element;
  //   let num_medida = String(num_med)
  //   container_style.style.width = num_medida.concat(med)
  // }

  //   HeightManager(element:HTMLElement,num_med:number,med:string){
  //   let container_style = element;
  //   let num_medida = String(num_med)
  //   container_style.style.height = num_medida.concat(med)
  // }

  


  
}
